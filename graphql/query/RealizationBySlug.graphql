query RealizationBySlug($slug: String!) {
  realizationBySlug: allRealizations(
    where: { slug: { current: { eq: $slug } } }
  ) {
    name
    images {
      asset {
        url
      }
    }
  }
}
